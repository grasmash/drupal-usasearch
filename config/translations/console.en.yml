command:
    usasearch:
        delete:
            description: 'Delete nodes from the i14y Search Index'
            arguments:
                id: 'What is the node id of the document?'
            options:
                all: 'If set, ALL nodes will be deleted.'
            messages:
              canceled: 'Canceled'
              missing: 'Missing arguements. Canceled'
            questions:
              confirm: 'Are you sure you want to delete ALL nodes from i14y?'
